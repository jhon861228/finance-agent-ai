{
  "name": "frontend",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:lambda": "astro build && cp server.mjs dist/ && cd dist && npm init -y && npm pkg set type=\"module\" && npm install express serverless-http --no-package-lock && zip -qr ../frontend_lambda.zip .",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/node": "^9.5.4",
    "astro": "^5.17.2",
    "express": "^5.2.1",
    "serverless-http": "^4.0.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6"
  }
}